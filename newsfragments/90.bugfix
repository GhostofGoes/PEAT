Release builds now report the release version: the release workflow builds the executables, Python package, and bundled documentation before it creates the release tag, so ``peat --version``, the wheel filename, the Windows executable metadata, and the documentation showed the previous release's version. The workflow now stamps the requested tag into the build via ``PDM_BUILD_SCM_VERSION``.
